# 파라미터 vs 하이퍼파라미터 완벽 가이드

## 목차
1. [기본 개념](#1-기본-개념)
2. [파라미터 vs 하이퍼파라미터 비교](#2-파라미터-vs-하이퍼파라미터-비교)
3. [모델별 파라미터와 하이퍼파라미터](#3-모델별-파라미터와-하이퍼파라미터)
4. [하이퍼파라미터 튜닝 방법](#4-하이퍼파라미터-튜닝-방법)

---

## 1. 기본 개념

### 1.1 파라미터 (Parameter)

**정의**: 모델이 학습 과정에서 **자동으로** 학습하여 결정되는 값

**특징**:
- 데이터로부터 학습됨
- 모델 내부 변수
- 학습 알고리즘이 최적값을 찾음
- 예측에 직접 사용됨
- 학습이 끝나면 고정됨

**예시**:
```
선형회귀: y = w₁x₁ + w₂x₂ + ... + b

파라미터:
- 가중치(weights): w₁, w₂, ...
- 편향(bias): b

→ 경사하강법으로 자동 학습
```

### 1.2 하이퍼파라미터 (Hyperparameter)

**정의**: 학습 과정을 제어하기 위해 **사용자가 직접** 설정하는 값

**특징**:
- 학습 전에 미리 설정
- 모델 외부 변수
- 모델의 성능과 학습 과정에 영향
- 데이터로부터 학습되지 않음
- 튜닝이 필요함

**예시**:
```
신경망 학습:

하이퍼파라미터:
- 학습률(learning rate): 0.001
- 배치 크기(batch size): 32
- 에포크(epochs): 100
- 은닉층 수: 3
- 각 층의 노드 수: [128, 64, 32]

→ 사용자가 직접 설정
```

---

## 2. 파라미터 vs 하이퍼파라미터 비교

### 2.1 핵심 차이점

| 구분 | 파라미터 | 하이퍼파라미터 |
|------|---------|---------------|
| **설정 주체** | 알고리즘이 자동 학습 | 사용자가 수동 설정 |
| **학습 여부** | 학습됨 | 학습되지 않음 |
| **설정 시점** | 학습 중 | 학습 전 |
| **영향 범위** | 예측 결과에 직접 영향 | 학습 과정과 최종 성능에 영향 |
| **값의 특성** | 데이터 의존적 | 문제와 도메인 의존적 |
| **조정 방법** | 최적화 알고리즘 | 튜닝 (Grid Search, Random Search 등) |
| **개수** | 일반적으로 많음 (수천~수백만) | 일반적으로 적음 (수개~수십개) |

### 2.2 관계도

```
하이퍼파라미터 설정
    ↓
┌─────────────────────────┐
│   학습 과정              │
│   (Training Process)    │
│                         │
│   데이터 + 모델 구조    │
│         ↓               │
│   최적화 알고리즘        │
│         ↓               │
│   파라미터 업데이트      │
│   (w, b 학습)           │
└─────────────────────────┘
    ↓
학습된 모델 (파라미터 확정)
    ↓
예측/추론
```

### 2.3 실제 예시 비교

```python
# 선형회귀 예시

# 하이퍼파라미터 (사용자 설정)
learning_rate = 0.01        # 학습률
iterations = 1000           # 반복 횟수
regularization = 'L2'       # 정규화 방법
alpha = 0.1                 # 정규화 강도

# 파라미터 (학습으로 결정)
# 초기값
w = np.random.randn(n_features)
b = 0

# 학습 과정
for i in range(iterations):
    # 예측
    y_pred = w @ X + b
    
    # 손실 계산
    loss = mse(y, y_pred)
    
    # 파라미터 업데이트 (자동 학습)
    w = w - learning_rate * gradient_w
    b = b - learning_rate * gradient_b

# 최종 학습된 파라미터
print(f"학습된 가중치: {w}")
print(f"학습된 편향: {b}")
```

---

## 3. 모델별 파라미터와 하이퍼파라미터

### 3.1 선형 회귀 (Linear Regression)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 가중치 (w₁, w₂, ..., wₙ) | 각 특징의 계수 |
| | 편향 (b) | 절편 |
| **하이퍼파라미터** | 학습률 (learning rate) | 경사하강법 사용 시 |
| | 정규화 유형 (None/L1/L2/Elastic) | Ridge, Lasso 등 |
| | 정규화 강도 (α, λ) | 정규화 페널티 크기 |
| | 반복 횟수 (iterations) | 경사하강법 사용 시 |
| | fit_intercept | 절편 포함 여부 |

**수식**:
```
모델: ŷ = w₁x₁ + w₂x₂ + ... + wₙxₙ + b

파라미터: w = [w₁, w₂, ..., wₙ], b
하이퍼파라미터: α (정규화 강도)

Loss = MSE + α·||w||² (L2 정규화)
```

### 3.2 로지스틱 회귀 (Logistic Regression)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 가중치 (w) | 선형 결합 계수 |
| | 편향 (b) | 절편 |
| **하이퍼파라미터** | C (역정규화 강도) | 1/λ, 클수록 정규화 약함 |
| | penalty ('l1', 'l2', 'elasticnet') | 정규화 유형 |
| | solver ('liblinear', 'saga', 'lbfgs') | 최적화 알고리즘 |
| | max_iter | 최대 반복 횟수 |
| | class_weight | 클래스 가중치 |

**수식**:
```
모델: P(y=1|x) = σ(w·x + b) = 1/(1+e^(-(w·x+b)))

파라미터: w, b
하이퍼파라미터: C, penalty
```

### 3.3 의사결정나무 (Decision Tree)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 분할 지점 (split points) | 각 노드의 분할 기준값 |
| | 분할 특징 (split features) | 어떤 특징으로 분할할지 |
| | 리프 노드 값 | 최종 예측값 |
| | 트리 구조 | 노드 연결 관계 |
| **하이퍼파라미터** | max_depth | 최대 트리 깊이 |
| | min_samples_split | 노드 분할 최소 샘플 수 |
| | min_samples_leaf | 리프 노드 최소 샘플 수 |
| | max_features | 분할 시 고려할 최대 특징 수 |
| | criterion ('gini', 'entropy') | 불순도 측정 방법 |
| | max_leaf_nodes | 최대 리프 노드 수 |
| | min_impurity_decrease | 분할 최소 불순도 감소량 |

**트리 구조 예시**:
```
              [X₁ < 5.0]  ← 파라미터 (학습으로 결정)
              /         \
         [X₂ < 3.0]    [Class B]
         /        \
    [Class A]  [Class C]

하이퍼파라미터:
- max_depth = 2
- min_samples_split = 10
- criterion = 'gini'
```

### 3.4 랜덤 포레스트 (Random Forest)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 각 트리의 분할 지점 | 개별 트리의 파라미터들 |
| | 각 트리의 구조 | 트리별 노드 구성 |
| **하이퍼파라미터** | n_estimators | 트리 개수 |
| | max_depth | 각 트리의 최대 깊이 |
| | min_samples_split | 노드 분할 최소 샘플 수 |
| | min_samples_leaf | 리프 노드 최소 샘플 수 |
| | max_features ('auto', 'sqrt', 'log2') | 분할 시 고려할 특징 수 |
| | bootstrap (True/False) | 부트스트랩 샘플링 여부 |
| | max_samples | 각 트리의 샘플 비율 |
| | criterion ('gini', 'entropy') | 불순도 측정 방법 |
| | oob_score | Out-of-bag 평가 여부 |

**앙상블 구조**:
```
Bootstrap 샘플 → 트리1 (파라미터₁) ┐
Bootstrap 샘플 → 트리2 (파라미터₂) ├→ 평균/투표
Bootstrap 샘플 → 트리3 (파라미터₃) ┘

하이퍼파라미터: n_estimators=3, max_depth=10, ...
```

### 3.5 서포트 벡터 머신 (SVM)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 가중치 벡터 (w) | 결정 경계의 방향 |
| | 편향 (b) | 결정 경계의 위치 |
| | 서포트 벡터 | 결정 경계를 정의하는 샘플들 |
| | α (라그랑주 승수) | 서포트 벡터의 중요도 |
| **하이퍼파라미터** | C | 오분류 페널티 강도 |
| | kernel ('linear', 'rbf', 'poly', 'sigmoid') | 커널 함수 유형 |
| | gamma ('scale', 'auto', float) | RBF/poly/sigmoid 커널 계수 |
| | degree | polynomial 커널 차수 |
| | coef0 | poly/sigmoid 커널 상수항 |

**수식**:
```
선형 SVM:
f(x) = sign(w·x + b)

파라미터: w, b (최적화로 학습)
하이퍼파라미터: C, kernel, gamma

RBF 커널:
K(x, x') = exp(-gamma·||x-x'||²)
```

### 3.6 K-최근접 이웃 (KNN)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 학습 데이터 자체 | 전체 학습 데이터 저장 |
| **하이퍼파라미터** | n_neighbors (k) | 고려할 이웃 개수 |
| | weights ('uniform', 'distance') | 이웃 가중치 방법 |
| | algorithm ('auto', 'ball_tree', 'kd_tree', 'brute') | 탐색 알고리즘 |
| | metric ('euclidean', 'manhattan', 'minkowski') | 거리 측정 방법 |
| | p | Minkowski 거리의 차수 |
| | leaf_size | Tree 알고리즘의 리프 크기 |

**특징**:
```
KNN은 게으른 학습(Lazy Learning)
→ 학습 단계에서 파라미터를 학습하지 않음
→ 학습 데이터 자체가 "파라미터" 역할

예측 시:
1. 테스트 샘플과 모든 학습 샘플의 거리 계산
2. 가장 가까운 k개 선택 (k는 하이퍼파라미터)
3. 다수결 또는 평균으로 예측
```

### 3.7 나이브 베이즈 (Naive Bayes)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | P(y) | 클래스 사전 확률 |
| | P(xᵢ\|y) | 조건부 확률 (특징의 우도) |
| | μ, σ² (Gaussian NB) | 평균과 분산 |
| | θ (Multinomial NB) | 특징 확률 |
| **하이퍼파라미터** | alpha (Laplace smoothing) | 평활화 파라미터 |
| | fit_prior | 클래스 사전 확률 학습 여부 |
| | var_smoothing (Gaussian) | 분산 평활화 |

**수식**:
```
Naive Bayes:
P(y|x) ∝ P(y)·∏P(xᵢ|y)

파라미터 (데이터로부터 추정):
- P(y): 클래스 빈도로 추정
- P(xᵢ|y): 조건부 빈도로 추정

하이퍼파라미터:
- alpha: P(xᵢ|y) = (count + alpha) / (total + alpha·n_features)
```

### 3.8 인공신경망 (Neural Network / MLP)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 가중치 (W¹, W², ..., Wᴸ) | 각 층의 가중치 행렬 |
| | 편향 (b¹, b², ..., bᴸ) | 각 층의 편향 벡터 |
| | Batch Norm 파라미터 (γ, β) | 스케일, 시프트 파라미터 |
| **하이퍼파라미터** | 네트워크 구조 | 층 수, 각 층의 노드 수 |
| | 학습률 (learning_rate) | 가중치 업데이트 크기 |
| | 배치 크기 (batch_size) | 한 번에 처리할 샘플 수 |
| | 에포크 (epochs) | 전체 데이터 학습 반복 횟수 |
| | 활성화 함수 | ReLU, Sigmoid, Tanh 등 |
| | 옵티마이저 | SGD, Adam, RMSProp 등 |
| | 손실 함수 | MSE, Cross-Entropy 등 |
| | dropout 비율 | 드롭아웃 비율 |
| | L2 정규화 강도 (lambda) | 가중치 감쇠 |
| | momentum | 모멘텀 계수 |
| | beta1, beta2 (Adam) | Adam의 모멘텀 파라미터 |

**구조 예시**:
```
네트워크 구조 (하이퍼파라미터):
입력층(4) → 은닉층1(64, ReLU) → 은닉층2(32, ReLU) → 출력층(3, Softmax)

파라미터 (학습됨):
W¹: [4 × 64]    b¹: [64]
W²: [64 × 32]   b²: [32]
W³: [32 × 3]    b³: [3]

총 파라미터 수 = 4×64 + 64 + 64×32 + 32 + 32×3 + 3 = 2,531개
```

### 3.9 XGBoost / Gradient Boosting

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 각 트리의 가중치 (wₜ) | 트리의 기여도 |
| | 각 트리의 구조 | 분할 지점, 리프 값 |
| | 리프 노드 점수 | 각 리프의 예측값 |
| **하이퍼파라미터** | n_estimators | 부스팅 라운드 수 (트리 개수) |
| | learning_rate (eta) | 학습률 (0.01~0.3) |
| | max_depth | 트리 최대 깊이 |
| | min_child_weight | 자식 노드 최소 가중치 합 |
| | subsample | 행 샘플링 비율 |
| | colsample_bytree | 열 샘플링 비율 |
| | gamma | 분할 최소 손실 감소 |
| | reg_alpha (L1) | L1 정규화 |
| | reg_lambda (L2) | L2 정규화 |
| | objective | 손실 함수 |
| | scale_pos_weight | 불균형 데이터 가중치 |

**부스팅 과정**:
```
F₀(x) = 초기 예측

F₁(x) = F₀(x) + η·h₁(x)  ← η: 하이퍼파라미터
F₂(x) = F₁(x) + η·h₂(x)     h₁, h₂: 파라미터 (트리)
...
Fₜ(x) = Fₜ₋₁(x) + η·hₜ(x)

최종: F(x) = Σ(η·hₜ(x))
```

### 3.10 LightGBM

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 트리 구조 | 리프 값, 분할 지점 |
| | 트리 가중치 | 각 트리의 기여도 |
| **하이퍼파라미터** | n_estimators | 트리 개수 |
| | learning_rate | 학습률 |
| | max_depth | 트리 깊이 (-1: 제한 없음) |
| | num_leaves | 리프 노드 최대 개수 |
| | min_data_in_leaf | 리프의 최소 샘플 수 |
| | feature_fraction | 특징 샘플링 비율 |
| | bagging_fraction | 데이터 샘플링 비율 |
| | bagging_freq | 배깅 빈도 |
| | lambda_l1, lambda_l2 | 정규화 |
| | boosting_type ('gbdt', 'dart', 'goss') | 부스팅 유형 |

### 3.11 K-Means 클러스터링

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 중심점 좌표 (μ₁, μ₂, ..., μₖ) | 각 클러스터의 중심 |
| **하이퍼파라미터** | n_clusters (k) | 클러스터 개수 |
| | init ('k-means++', 'random') | 초기 중심점 설정 방법 |
| | n_init | 다른 초기값으로 실행 횟수 |
| | max_iter | 최대 반복 횟수 |
| | tol | 수렴 허용 오차 |
| | algorithm ('auto', 'full', 'elkan') | 알고리즘 선택 |

**알고리즘**:
```
1. k개 초기 중심점 선택 (하이퍼파라미터: k, init)
2. 반복 (하이퍼파라미터: max_iter):
   a. 각 샘플을 가장 가까운 중심점에 할당
   b. 각 클러스터의 중심점 재계산 (파라미터 업데이트)
3. 수렴까지 반복

최종 파라미터: μ₁, μ₂, ..., μₖ
```

### 3.12 주성분 분석 (PCA)

| 구분 | 내용 | 설명 |
|------|------|------|
| **파라미터** | 주성분 (PC₁, PC₂, ..., PCₖ) | 고유벡터 |
| | 설명 분산 (λ₁, λ₂, ..., λₖ) | 고유값 |
| | 평균 벡터 (μ) | 데이터 중심화 |
| **하이퍼파라미터** | n_components | 주성분 개수 또는 설명 분산 비율 |
| | svd_solver ('auto', 'full', 'arpack', 'randomized') | SVD 알고리즘 |
| | whiten | 백색화 여부 |

**수식**:
```
PCA 변환:
X_transformed = (X - μ)·V

파라미터 (데이터로부터 계산):
- μ: 평균 벡터
- V: 주성분 (고유벡터)
- λ: 설명 분산 (고유값)

하이퍼파라미터:
- n_components: 몇 개의 주성분 사용할지
```

---

## 4. 하이퍼파라미터 튜닝 방법

### 4.1 주요 튜닝 기법 비교

| 방법 | 설명 | 장점 | 단점 | 사용 시나리오 |
|------|------|------|------|--------------|
| **Grid Search** | 모든 조합 탐색 | 최적값 보장 (범위 내) | 계산 비용 높음 | 하이퍼파라미터 적고 범위 좁을 때 |
| **Random Search** | 무작위 샘플링 | Grid보다 효율적 | 최적값 보장 안 됨 | 하이퍼파라미터 많을 때 |
| **Bayesian Optimization** | 확률적 모델 기반 | 효율적, 적은 시도 | 구현 복잡 | 평가 비용 높을 때 |
| **Halving Grid/Random** | 점진적 자원 할당 | 빠름 | scikit-learn 최신 버전 필요 | 큰 데이터셋 |
| **Optuna** | 자동 최적화 프레임워크 | 강력함, 시각화 | 외부 라이브러리 | 복잡한 최적화 |

### 4.2 Grid Search 예시

```python
from sklearn.model_selection import GridSearchCV
from sklearn.ensemble import RandomForestClassifier

# 하이퍼파라미터 그리드 정의
param_grid = {
    'n_estimators': [50, 100, 200],           # 트리 개수
    'max_depth': [10, 20, 30, None],          # 트리 깊이
    'min_samples_split': [2, 5, 10],          # 분할 최소 샘플
    'min_samples_leaf': [1, 2, 4],            # 리프 최소 샘플
    'max_features': ['sqrt', 'log2']          # 특징 개수
}

# 모델 생성
rf = RandomForestClassifier(random_state=42)

# Grid Search
grid_search = GridSearchCV(
    estimator=rf,
    param_grid=param_grid,
    cv=5,                    # 5-fold 교차검증
    scoring='accuracy',
    n_jobs=-1,              # 병렬 처리
    verbose=2
)

# 실행
grid_search.fit(X_train, y_train)

# 최적 하이퍼파라미터
print("최적 파라미터:", grid_search.best_params_)
print("최적 점수:", grid_search.best_score_)

# 총 시도 횟수: 3 × 4 × 3 × 3 × 2 = 216 조합
```

### 4.3 Random Search 예시

```python
from sklearn.model_selection import RandomizedSearchCV
from scipy.stats import randint, uniform

# 하이퍼파라미터 분포 정의
param_distributions = {
    'n_estimators': randint(50, 500),           # 50~500 사이 정수
    'max_depth': randint(5, 50),                # 5~50 사이 정수
    'min_samples_split': randint(2, 20),        # 2~20 사이 정수
    'min_samples_leaf': randint(1, 10),         # 1~10 사이 정수
    'max_features': uniform(0.1, 0.9)           # 0.1~1.0 사이 실수
}

# Random Search
random_search = RandomizedSearchCV(
    estimator=rf,
    param_distributions=param_distributions,
    n_iter=100,              # 100번 시도
    cv=5,
    scoring='accuracy',
    n_jobs=-1,
    random_state=42,
    verbose=2
)

random_search.fit(X_train, y_train)

# 총 시도 횟수: 100번 (Grid보다 훨씬 적음)
```

### 4.4 Bayesian Optimization 예시

```python
from sklearn.model_selection import cross_val_score
from skopt import BayesSearchCV
from skopt.space import Real, Integer

# 탐색 공간 정의
search_spaces = {
    'n_estimators': Integer(50, 500),
    'max_depth': Integer(5, 50),
    'min_samples_split': Integer(2, 20),
    'min_samples_leaf': Integer(1, 10),
    'max_features': Real(0.1, 1.0)
}

# Bayesian Optimization
bayes_search = BayesSearchCV(
    estimator=rf,
    search_spaces=search_spaces,
    n_iter=50,               # 50번만 시도
    cv=5,
    scoring='accuracy',
    n_jobs=-1,
    random_state=42
)

bayes_search.fit(X_train, y_train)

# 50번으로 최적에 가까운 결과 도출
```

### 4.5 하이퍼파라미터 튜닝 전략

```
단계별 접근:

1단계: 기본값으로 시작
   - 대부분의 라이브러리는 합리적인 기본값 제공
   - 베이스라인 성능 확인

2단계: 중요한 하이퍼파라미터 식별
   - 모델별 주요 하이퍼파라미터 파악
   - 영향력이 큰 것부터 튜닝

3단계: 넓은 범위에서 탐색
   - Random Search로 대략적인 범위 파악
   - 10^-4 ~ 10^2 등 로그 스케일 사용

4단계: 좁은 범위에서 정밀 탐색
   - Grid Search로 세밀하게 탐색
   - 3단계에서 찾은 좋은 값 주변 탐색

5단계: 교차 검증으로 평가
   - K-Fold CV로 일반화 성능 평가
   - 과적합 여부 확인

6단계: 최종 모델 선택
   - 검증 데이터로 최종 확인
   - 테스트 데이터는 마지막에만 사용
```

### 4.6 모델별 주요 튜닝 우선순위

#### Random Forest
```
1순위: n_estimators (많을수록 좋음, 단 계산 비용 고려)
2순위: max_depth (과적합 방지)
3순위: min_samples_split, min_samples_leaf
4순위: max_features
```

#### XGBoost
```
1순위: learning_rate + n_estimators (함께 조정)
2순위: max_depth (3~10)
3순위: subsample, colsample_bytree (0.6~1.0)
4순위: reg_alpha, reg_lambda (정규화)
```

#### 신경망
```
1순위: 네트워크 구조 (층 수, 노드 수)
2순위: learning_rate (0.0001~0.1)
3순위: batch_size (32, 64, 128)
4순위: dropout 비율 (0.2~0.5)
5순위: optimizer (Adam이 일반적으로 좋음)
```

#### SVM
```
1순위: kernel 선택 (linear, rbf)
2순위: C (0.1~1000, 로그 스케일)
3순위: gamma (rbf 커널 사용 시, 0.001~1)
```

### 4.7 하이퍼파라미터 튜닝 체크리스트

```
□ 데이터 전처리 완료
  - 스케일링/정규화
  - 결측치 처리
  - 범주형 인코딩

□ 훈련/검증/테스트 분할
  - 테스트 세트는 절대 튜닝에 사용 금지
  - 교차 검증 활용

□ 베이스라인 성능 측정
  - 기본 설정으로 성능 확인

□ 탐색 범위 설정
  - 너무 좁으면 최적값 놓침
  - 너무 넓으면 계산 비용 증가

□ 시간/자원 고려
  - n_iter, cv 수 조정
  - 병렬 처리 활용 (n_jobs=-1)

□ 결과 분석
  - learning curve 확인
  - 과적합/과소적합 판단

□ 최종 평가
  - 테스트 세트로 최종 성능 확인
```

---

## 5. 정리 및 요약

### 5.1 핵심 포인트

**파라미터**:
- ✅ 모델이 **자동으로 학습**
- ✅ 예측에 **직접 사용**됨
- ✅ 수천~수백만 개 (복잡한 모델)
- ✅ 데이터 의존적

**하이퍼파라미터**:
- ✅ 사용자가 **수동으로 설정**
- ✅ 학습 과정 **제어**
- ✅ 수개~수십 개
- ✅ 도메인 지식 필요

### 5.2 빅데이터분석기사 시험 대비

**자주 출제되는 개념**:
1. 파라미터와 하이퍼파라미터의 정의 차이
2. 각 모델의 대표적인 하이퍼파라미터 (특히 트리 기반 모델)
3. Grid Search vs Random Search 차이
4. 교차 검증의 중요성
5. 학습률, 정규화 강도 같은 주요 하이퍼파라미터의 역할

**예상 문제 유형**:
```
Q: 다음 중 하이퍼파라미터가 아닌 것은?
   ① Random Forest의 max_depth
   ② 선형회귀의 가중치 w
   ③ SVM의 C
   ④ KNN의 k

A: ② (가중치는 학습으로 결정되는 파라미터)
```

### 5.3 모델 선택 가이드

| 데이터/상황 | 추천 모델 | 주요 튜닝 대상 |
|------------|----------|---------------|
| 표 형태, 중소규모 | Random Forest, XGBoost | n_estimators, max_depth, learning_rate |
| 표 형태, 대규모 | LightGBM | num_leaves, learning_rate |
| 이미지 데이터 | CNN | 네트워크 구조, learning_rate, dropout |
| 시계열 데이터 | LSTM, GRU | 네트워크 구조, sequence_length |
| 텍스트 데이터 | Transformer | learning_rate, num_layers, hidden_size |
| 선형 관계 명확 | Linear/Logistic Regression | 정규화 강도 |
| 고차원, 희소 | Linear SVM, Naive Bayes | C (SVM), alpha (NB) |

---

**시험 준비 화이팅! 🎯**

파라미터는 **학습되고**, 하이퍼파라미터는 **설정된다**는 점을 명확히 기억하세요!
