# 사회연결망분석 (Social Network Analysis) 완벽 가이드

## 목차
1. [기본 개념](#1-기본-개념)
2. [네트워크 구성요소](#2-네트워크-구성요소)
3. [중심성 지표](#3-중심성-지표)
4. [네트워크 구조 분석](#4-네트워크-구조-분석)
5. [주요 용어 정리](#5-주요-용어-정리)
6. [실전 응용](#6-실전-응용)
7. [Python 구현](#7-python-구현)

---

## 1. 기본 개념

### 1.1 사회연결망분석이란?

**정의**: 개체(노드) 간의 관계(엣지)를 네트워크로 표현하고, 구조적 패턴을 분석하여 개체의 위치, 영향력, 집단 특성 등을 파악하는 분석 방법

**핵심 아이디어**:
```
개체는 고립되어 있지 않고, 관계 속에서 존재한다.
관계의 패턴(구조)이 개체의 행동과 결과에 영향을 미친다.
```

### 1.2 적용 분야

| 분야 | 분석 대상 | 활용 예시 |
|------|----------|----------|
| **SNS 분석** | 사용자 간 관계 | 인플루언서 발굴, 정보 확산 경로 |
| **조직 분석** | 구성원 간 협업 관계 | 커뮤니케이션 패턴, 핵심 인물 파악 |
| **마케팅** | 고객-상품 관계 | 추천 시스템, 바이럴 마케팅 |
| **생물학** | 단백질 상호작용 | 질병 메커니즘 연구 |
| **교통** | 도시 간 연결 | 최적 경로, 허브 도시 분석 |
| **범죄 분석** | 조직원 간 관계 | 핵심 인물 식별, 조직 구조 파악 |

### 1.3 전통적 통계분석 vs 네트워크분석

| 구분 | 전통적 통계분석 | 네트워크분석 |
|------|----------------|-------------|
| **관점** | 개체의 속성(attribute) | 개체 간 관계(relation) |
| **가정** | 개체는 독립적 | 개체는 상호의존적 |
| **분석 단위** | 개별 개체 | 관계, 구조 |
| **주요 질문** | "누가 어떤 특성?" | "누가 누구와 연결?" |
| **데이터 형태** | 행렬 (n×p) | 인접행렬 (n×n) |
| **예시** | 평균 소득, 회귀분석 | 중심성, 클러스터 |

---

## 2. 네트워크 구성요소

### 2.1 기본 구성요소

```
네트워크 = G(V, E)

V (Vertex/Node): 노드 (개체, 행위자)
E (Edge/Link): 엣지 (관계, 연결)
```

**시각화 예시**:
```
무방향 네트워크 (Undirected):
    A ——— B
    |     |
    |     |
    C ——— D

방향 네트워크 (Directed):
    A ←—— B
    ↓     ↓
    ↓     ↓
    C ——→ D

가중치 네트워크 (Weighted):
    A —5— B
    |     |
   3|     |7
    |     |
    C —2— D
```

### 2.2 네트워크 유형

| 유형 | 설명 | 예시 | 표현 |
|------|------|------|------|
| **무방향 (Undirected)** | 관계에 방향성 없음 | 친구 관계, 공동 저자 | A ——— B |
| **방향 (Directed)** | 관계에 방향성 있음 | 팔로우, 인용, 조언 | A ——→ B |
| **가중치 (Weighted)** | 관계의 강도 표현 | 통화 빈도, 거래액 | A —5— B |
| **이분 (Bipartite)** | 두 종류의 노드 | 고객-상품, 배우-영화 | A●—○X |
| **다중 (Multiplex)** | 여러 유형의 관계 | SNS 친구+업무 동료 | 여러 층 |

### 2.3 인접행렬 (Adjacency Matrix)

**무방향 네트워크**:
```
네트워크:
    A ——— B
    |     |
    C ——— D

인접행렬:
      A  B  C  D
   A [0  1  1  0]
   B [1  0  0  1]
   C [1  0  0  1]
   D [0  1  1  0]

특징:
- 대칭 행렬 (i,j) = (j,i)
- 대각선은 보통 0 (self-loop 없으면)
```

**방향 네트워크**:
```
네트워크:
    A ←— B
    ↓    ↓
    C —→ D

인접행렬:
      A  B  C  D
   A [0  0  1  0]  ← A는 C에게만 화살표
   B [1  0  0  1]  ← B는 A, D에게 화살표
   C [0  0  0  1]  ← C는 D에게만 화살표
   D [0  0  0  0]  ← D는 아무에게도 화살표 없음

특징:
- 비대칭 행렬
- 행(i): i가 보내는 연결 (out-degree)
- 열(j): j가 받는 연결 (in-degree)
```

**가중치 네트워크**:
```
인접행렬:
      A  B  C  D
   A [0  5  3  0]
   B [5  0  0  7]
   C [3  0  0  2]
   D [0  7  2  0]

값이 관계의 강도를 나타냄
```

---

## 3. 중심성 지표

### 3.1 중심성(Centrality)이란?

**정의**: 네트워크에서 특정 노드가 얼마나 중심적인 위치에 있는지를 나타내는 지표

**4대 중심성**:
1. 연결 중심성 (Degree Centrality)
2. 근접 중심성 (Closeness Centrality)
3. 매개 중심성 (Betweenness Centrality)
4. 위세 중심성 (Eigenvector Centrality)

### 3.2 연결 중심성 (Degree Centrality)

**개념**: 한 노드에 직접 연결된 이웃 노드의 수

**수식**:
```
무방향 네트워크:
CD(i) = ki / (n-1)

방향 네트워크:
- In-Degree: CD_in(i) = ki_in / (n-1)
- Out-Degree: CD_out(i) = ki_out / (n-1)

여기서:
- ki: 노드 i의 연결 수 (degree)
- n: 전체 노드 수
- (n-1): 최대 가능 연결 수 (정규화)
```

**예시**:
```
네트워크:
       B
      /|\
     / | \
    A--C--D
       |
       E

연결 수:
A: 2개 (B, C)
B: 3개 (A, C, D)
C: 4개 (A, B, D, E) ← 최고 연결 중심성
D: 2개 (B, C)
E: 1개 (C)

정규화 (n=5, 최대=4):
CD(C) = 4/4 = 1.0
CD(B) = 3/4 = 0.75
```

**의미**:
- 높은 연결 중심성 → 많은 직접 연결 → 정보 접근 용이, 영향력
- SNS: 친구가 많은 사람
- 조직: 많은 사람과 직접 소통하는 사람

### 3.3 근접 중심성 (Closeness Centrality)

**개념**: 한 노드에서 다른 모든 노드까지의 최단 거리 합의 역수

**수식**:
```
CC(i) = (n-1) / Σd(i,j)

여기서:
- d(i,j): 노드 i에서 j까지의 최단 거리
- Σd(i,j): i에서 모든 다른 노드까지 거리의 합
- (n-1): 정규화 상수
```

**예시**:
```
네트워크:
A --- B --- C --- D --- E

거리 계산 (노드 C 기준):
- C → A: 2 (C-B-A)
- C → B: 1 (C-B)
- C → D: 1 (C-D)
- C → E: 2 (C-D-E)
총합: 2+1+1+2 = 6

CC(C) = 4/6 = 0.667 ← 가장 높음 (중앙에 위치)

노드 A 기준:
- A → B: 1
- A → C: 2
- A → D: 3
- A → E: 4
총합: 10

CC(A) = 4/10 = 0.4 ← 낮음 (끝에 위치)
```

**의미**:
- 높은 근접 중심성 → 다른 노드들과 가까움 → 정보 빠르게 전파/수신
- 조직: 정보를 빨리 전달/수집할 수 있는 위치
- 물류: 배송 허브 역할

### 3.4 매개 중심성 (Betweenness Centrality)

**개념**: 네트워크 내 다른 노드들 간의 최단 경로가 특정 노드를 거치는 정도

**수식**:
```
CB(i) = Σ(gjk(i) / gjk)

여기서:
- gjk: 노드 j와 k 사이의 최단 경로 수
- gjk(i): j와 k 사이 최단 경로 중 i를 거치는 경로 수
- 모든 j, k 쌍에 대해 합산 (j≠k≠i)
```

**예시**:
```
네트워크:
A --- B --- C
      |     |
      D --- E

노드 B의 매개 중심성:
- A와 C 사이: A-B-C (B를 거침) ✓
- A와 D 사이: A-B-D (B를 거침) ✓
- A와 E 사이: A-B-C-E 또는 A-B-D-E (B를 거침) ✓
- C와 D 사이: C-B-D (B를 거침) ✓
- 기타 경로들도 대부분 B를 거침

→ B는 높은 매개 중심성 (다리 역할)

노드 E의 매개 중심성:
- 대부분의 최단 경로가 E를 거치지 않음
→ E는 낮은 매개 중심성
```

**의미**:
- 높은 매개 중심성 → 정보 흐름 통제 가능 → 브로커, 게이트키퍼 역할
- 조직: 부서 간 연결 고리
- 네트워크: 병목 지점, 취약점
- 제거 시 네트워크 단절 가능성

### 3.5 위세 중심성 (Eigenvector Centrality)

**개념**: 영향력 있는 노드와 연결될수록 높은 점수 (연결의 질 고려)

**수식**:
```
λ·x = A·x

xi = (1/λ) Σ Aij·xj

여기서:
- xi: 노드 i의 위세 중심성
- λ: 인접행렬 A의 최대 고유값
- Aij: 인접행렬 원소
- 반복 계산으로 수렴
```

**직관적 설명**:
```
"중요한 사람과 친구인 것이 중요하다"

Case 1:
  당신 --- (유명인 3명)
  위세 중심성: 높음

Case 2:
  당신 --- (무명인 10명)
  위세 중심성: 낮을 수 있음

→ 연결 수보다 연결 상대의 중요도 반영
```

**예시**:
```
네트워크:
     A (유명)
    /|\
   B C D (일반인)
     |
     E (일반인)

위세 중심성 순위:
1. A (높음): 많이 연결됨
2. C (중간): A와 연결 + E와도 연결
3. B, D (낮음): A와만 연결
4. E (매우 낮음): C와만 연결, C도 그다지 중심적이지 않음
```

**의미**:
- 높은 위세 중심성 → 영향력 있는 노드들과 연결
- SNS: 셀럽과 연결된 사람
- 학술: 인용 많이 받는 논문을 인용하는 논문
- Google PageRank의 기반 아이디어

### 3.6 중심성 지표 비교표

| 중심성 | 측정 대상 | 높은 점수의 의미 | 계산 복잡도 | 적용 예시 |
|--------|----------|-----------------|------------|-----------|
| **연결 중심성** | 직접 연결 수 | 많은 직접 연결 | O(n) | SNS 친구 많은 사람 |
| **근접 중심성** | 평균 최단 거리 | 다른 노드와 가까움 | O(n²) | 정보 빨리 전파 |
| **매개 중심성** | 최단 경로 통과 | 정보 흐름 통제 | O(n³) | 부서 간 연결 고리 |
| **위세 중심성** | 중요 노드와 연결 | 영향력 있는 연결 | 반복 계산 | 셀럽과 친한 사람 |

### 3.7 중심성 시각화 비교

```
같은 네트워크, 다른 중심성:

네트워크:
    1---2---3
    |   |   |
    4---5---6
        |
        7

연결 중심성:
5번 노드가 최고 (4개 연결)
2번, 4번, 6번도 높음 (3개)

근접 중심성:
5번 노드가 최고 (중앙)
3번, 5번이 높음

매개 중심성:
5번 노드가 최고 (모든 경로의 중심)
2번도 높음 (1-3 연결)

위세 중심성:
5번과 연결된 노드들이 높음
5번 자체도 높음
```

---

## 4. 네트워크 구조 분석

### 4.1 밀도 (Density)

**정의**: 실제 연결 수 / 가능한 최대 연결 수

**수식**:
```
무방향 네트워크:
Density = 2L / (n(n-1))

방향 네트워크:
Density = L / (n(n-1))

여기서:
- L: 실제 엣지 수
- n: 노드 수
- n(n-1)/2: 무방향 최대 엣지 수
- n(n-1): 방향 최대 엣지 수
```

**예시**:
```
네트워크 (n=4):
A --- B
|     |
C --- D

L = 4 (A-B, B-D, D-C, C-A)
최대 = 4×3/2 = 6

Density = 2×4 / (4×3) = 8/12 = 0.667 (66.7%)
```

**해석**:
- 0에 가까움: 희소 네트워크 (sparse)
- 1에 가까움: 밀집 네트워크 (dense)
- 높은 밀도: 강한 응집력, 정보 빠른 전파
- 낮은 밀도: 느슨한 연결, 다양성

### 4.2 클러스터링 계수 (Clustering Coefficient)

**정의**: 한 노드의 이웃들끼리도 서로 연결된 정도

**수식**:
```
국소 클러스터링 계수 (Local):
Ci = 2ei / (ki(ki-1))

여기서:
- ei: 노드 i의 이웃들 사이의 실제 연결 수
- ki: 노드 i의 연결 수 (degree)
- ki(ki-1)/2: 이웃들 사이의 최대 가능 연결 수

전역 클러스터링 계수 (Global):
C = (1/n) Σ Ci
```

**예시**:
```
노드 A의 이웃: B, C, D

Case 1: 이웃들이 모두 연결
    B---C
    |\ /|
    | A |
    |/ \|
    D---+

A의 이웃 (B,C,D)끼리 연결:
B-C, C-D, D-B (3개)
최대 가능: 3 (3개 노드)
CA = 2×3 / (3×2) = 1.0 (완벽한 클러스터)

Case 2: 이웃들이 연결 안 됨
    B   C
     \ /
      A
     /
    D

A의 이웃끼리 연결: 0개
CA = 0 (클러스터 없음)
```

**의미**:
- 높은 클러스터링: "내 친구들도 서로 친구"
- 사회: 폐쇄적 그룹, 강한 유대
- 낮은 클러스터링: 다양한 그룹과 연결
- 브로커 역할 가능성

### 4.3 연결요소 (Connected Component)

**정의**: 서로 경로로 연결된 노드들의 집합

**유형**:
```
강한 연결요소 (Strongly Connected):
- 방향 그래프에서
- 모든 노드 쌍이 양방향으로 도달 가능

약한 연결요소 (Weakly Connected):
- 방향 그래프에서
- 방향 무시하면 연결됨
```

**예시**:
```
네트워크:
[A---B---C]   [D---E]   [F]

3개의 연결요소:
1. {A, B, C}
2. {D, E}
3. {F} (고립 노드)
```

**최대 연결요소 (Giant Component)**:
- 네트워크에서 가장 큰 연결요소
- 전체 네트워크의 골격
- 정보 전파의 주요 경로

### 4.4 경로와 거리

| 용어 | 정의 | 예시 |
|------|------|------|
| **경로 (Path)** | 한 노드에서 다른 노드로 가는 엣지의 순서 | A→B→C |
| **경로 길이** | 경로상의 엣지 수 | 위 예시: 2 |
| **최단 경로** | 두 노드 사이의 가장 짧은 경로 | A→C (직접) vs A→B→C |
| **거리 (Distance)** | 최단 경로의 길이 | d(A,C) = 1 |
| **지름 (Diameter)** | 네트워크에서 가장 긴 최단 경로 | max d(i,j) |
| **평균 경로 길이** | 모든 노드 쌍 거리의 평균 | Σd(i,j) / (n(n-1)) |

**예시**:
```
네트워크:
A---B---C---D---E

최단 거리:
d(A,E) = 4
d(A,C) = 2
d(B,D) = 2

지름 (Diameter):
max = d(A,E) = 4

평균 경로 길이:
모든 쌍의 거리 평균 = 2.0
```

### 4.5 작은 세상 현상 (Small-World)

**정의**: 큰 네트워크임에도 평균 경로 길이가 짧은 현상

**6단계 분리 (Six Degrees of Separation)**:
- 전 세계 어느 두 사람도 평균 6단계로 연결
- SNS에서 더 짧아짐 (Facebook: 3.5단계)

**특징**:
```
작은 세상 네트워크:
- 낮은 평균 경로 길이
- 높은 클러스터링 계수
- 허브 노드 존재

랜덤 네트워크:
- 낮은 평균 경로 길이
- 낮은 클러스터링 계수

규칙적 네트워크:
- 높은 평균 경로 길이
- 높은 클러스터링 계수
```

### 4.6 척도 없는 네트워크 (Scale-Free)

**정의**: 연결 수 분포가 멱법칙(power law)을 따르는 네트워크

**멱법칙**:
```
P(k) ∝ k^(-γ)

여기서:
- P(k): degree가 k인 노드의 비율
- γ: 보통 2~3
- 대부분 노드: 적은 연결
- 소수 노드(허브): 매우 많은 연결
```

**특징**:
```
허브 존재:
- 소수의 고도로 연결된 노드
- "부익부 빈익빈"
- 새 노드는 이미 연결 많은 노드에 우선 연결
  (우선선택 Preferential Attachment)

예시:
- 웹 페이지: Google, Wikipedia 등
- SNS: 셀럽, 인플루언서
- 공항: 인천, 두바이, 싱가포르 등
- 인용: 자주 인용되는 논문
```

**시각화**:
```
연결 수 분포:

정규 분포 (Random):
빈도 ^
    |     ___
    |   /     \
    | /         \
    |/___________\___> 연결 수
       평균 주변 집중

멱법칙 (Scale-Free):
빈도 ^
    |●
    |●●
    |●●●
    |●●●●●●
    |●●●●●●●●●●●●____> 연결 수
    적은 연결 많음, 허브 소수
```

### 4.7 커뮤니티 탐지 (Community Detection)

**정의**: 네트워크를 밀접하게 연결된 하위 그룹으로 분할

**모듈성 (Modularity)**:
```
Q = (1/2L) Σ[Aij - (kikj/2L)]·δ(ci,cj)

여기서:
- L: 총 엣지 수
- Aij: 인접행렬
- ki, kj: 노드 i, j의 연결 수
- ci, cj: 노드 i, j의 커뮤니티
- δ(ci,cj): 같은 커뮤니티면 1, 아니면 0

Q가 클수록 좋은 커뮤니티 구조
```

**주요 알고리즘**:

| 알고리즘 | 방법 | 특징 |
|---------|------|------|
| **Girvan-Newman** | 매개 중심성 높은 엣지 제거 | 계층적, 느림 |
| **Louvain** | 모듈성 최적화 | 빠름, 많이 사용 |
| **Label Propagation** | 레이블 전파 | 매우 빠름, 비결정적 |
| **Spectral Clustering** | 고유벡터 사용 | 수학적 근거 |

**시각화**:
```
커뮤니티 탐지 전:
  ●—●—●—●—●—●
  |  \  /  \  |
  ●—●—●—●—●—●

커뮤니티 탐지 후:
  [●—●—●]—[●—●—●]
   커뮤니티1  커뮤니티2
```

---

## 5. 주요 용어 정리

### 5.1 기본 용어

| 용어 | 영문 | 정의 |
|------|------|------|
| **노드** | Node/Vertex | 네트워크의 개체 (사람, 웹페이지 등) |
| **엣지** | Edge/Link | 노드 간의 관계 (친구, 링크 등) |
| **연결 수** | Degree | 한 노드의 엣지 개수 |
| **이웃** | Neighbor | 직접 연결된 노드 |
| **경로** | Path | 한 노드에서 다른 노드로 가는 엣지 순서 |
| **거리** | Distance | 두 노드 간 최단 경로 길이 |
| **지름** | Diameter | 네트워크에서 가장 긴 최단 경로 |

### 5.2 네트워크 특성

| 용어 | 영문 | 정의 |
|------|------|------|
| **밀도** | Density | 실제 연결 / 가능한 최대 연결 |
| **클러스터링 계수** | Clustering Coefficient | 이웃들끼리 연결된 정도 |
| **연결요소** | Connected Component | 서로 연결된 노드 집합 |
| **평균 경로 길이** | Average Path Length | 모든 노드 쌍 거리의 평균 |
| **모듈성** | Modularity | 커뮤니티 구조의 강도 |

### 5.3 중심성 용어

| 용어 | 영문 | 측정 대상 |
|------|------|----------|
| **연결 중심성** | Degree Centrality | 직접 연결 수 |
| **근접 중심성** | Closeness Centrality | 다른 노드들과의 평균 거리 |
| **매개 중심성** | Betweenness Centrality | 최단 경로를 거치는 정도 |
| **위세 중심성** | Eigenvector Centrality | 중요한 노드와의 연결 |
| **페이지랭크** | PageRank | 위세 중심성의 변형 (웹) |

### 5.4 네트워크 유형

| 용어 | 영문 | 특징 |
|------|------|------|
| **랜덤 네트워크** | Random Network | 무작위 연결 |
| **작은 세상** | Small-World | 짧은 경로 + 높은 클러스터링 |
| **척도 없는** | Scale-Free | 멱법칙 분포, 허브 존재 |
| **계층적** | Hierarchical | 트리 구조 |
| **이분** | Bipartite | 두 종류 노드 |

---

## 6. 실전 응용

### 6.1 SNS 인플루언서 분석

**목적**: 영향력 있는 사용자 식별

**분석 절차**:
```
1. 네트워크 구축
   - 노드: 사용자
   - 엣지: 팔로우, 멘션, 리트윗

2. 중심성 계산
   - 연결 중심성: 팔로워 많은 사람
   - 위세 중심성: 인플루언서가 팔로우하는 사람
   - 매개 중심성: 정보 전파 핵심

3. 인플루언서 유형 분류
   - 셀럽형: 높은 연결 + 높은 위세
   - 브로커형: 높은 매개 중심성
   - 허브형: 높은 연결 + 높은 근접

4. 타겟 마케팅 전략 수립
```

**예시**:
```python
# 인플루언서 점수 계산
influencer_score = (
    0.3 * degree_centrality +
    0.3 * eigenvector_centrality +
    0.2 * betweenness_centrality +
    0.2 * closeness_centrality
)
```

### 6.2 추천 시스템

**협업 필터링 네트워크**:
```
사용자-상품 이분 네트워크:

사용자1 ——— 상품A
    |  \      |
    |    \    |
사용자2 ——— 상품B ——— 상품C
    |            |
사용자3 ——————————┘

추천 로직:
1. 사용자1과 유사한 사용자 찾기 (공통 상품 많은)
2. 그들이 구매한 상품 중 사용자1이 안 산 것 추천
3. 상품 간 유사도도 활용 (같이 많이 구매된)
```

### 6.3 조직 내 커뮤니케이션 분석

**분석 목표**:
- 부서 간 협업 패턴
- 정보 병목 지점 식별
- 핵심 인물 파악

**지표 활용**:
```
연결 중심성:
- 많은 사람과 소통하는 사람
- 정보 허브 역할

매개 중심성:
- 부서 간 연결 고리
- 제거 시 조직 단절 위험
- 정보 흐름 통제

클러스터링:
- 폐쇄적 그룹 식별
- 부서 간 장벽 파악

커뮤니티:
- 비공식 조직 구조
- 프로젝트 팀 구성
```

### 6.4 범죄 조직 분석

**목적**: 조직 구조 파악 및 핵심 인물 식별

**분석 방법**:
```
1. 네트워크 구축
   - 노드: 조직원
   - 엣지: 통화, 만남, 거래

2. 핵심 인물 식별
   - 연결 중심성: 많은 조직원과 연락
   - 매개 중심성: 조직 내 중개자
   - 위세 중심성: 보스와 가까운 사람

3. 타격 전략
   - 높은 매개 중심성 노드 제거
   → 조직 분열
   - 커뮤니티 단위 수사
```

### 6.5 질병 전파 분석

**목적**: 감염 경로 및 슈퍼전파자 식별

**네트워크 모델**:
```
SIR 모델 (Susceptible-Infected-Recovered):

시간 t=0:
S(감수성) ——— S ——— S
    |         |
    I(감염)   S

시간 t=1:
S ——— I ——— I ← 전파
    |     |
    I     S

시간 t=2:
R(회복) ——— I ——— I
    |         |
    R         I

분석:
- 높은 연결 중심성 노드: 감염 위험 높음
- 높은 매개 중심성 노드: 전파 핵심 경로
- 커뮤니티: 격리 단위
```

---

## 7. Python 구현

### 7.1 NetworkX 기본 사용법

```python
import networkx as nx
import matplotlib.pyplot as plt
import numpy as np

# 그래프 생성
G = nx.Graph()  # 무방향 그래프
# G = nx.DiGraph()  # 방향 그래프

# 노드 추가
G.add_node("A")
G.add_nodes_from(["B", "C", "D", "E"])

# 엣지 추가
G.add_edge("A", "B")
G.add_edges_from([("B", "C"), ("C", "D"), ("D", "E"), ("E", "A")])

# 가중치 엣지
G.add_edge("A", "C", weight=5)

# 기본 정보
print(f"노드 수: {G.number_of_nodes()}")
print(f"엣지 수: {G.number_of_edges()}")
print(f"노드 리스트: {list(G.nodes())}")
print(f"엣지 리스트: {list(G.edges())}")
```

### 7.2 중심성 계산

```python
# 연결 중심성
degree_centrality = nx.degree_centrality(G)
print("연결 중심성:", degree_centrality)

# 근접 중심성
closeness_centrality = nx.closeness_centrality(G)
print("근접 중심성:", closeness_centrality)

# 매개 중심성
betweenness_centrality = nx.betweenness_centrality(G)
print("매개 중심성:", betweenness_centrality)

# 위세 중심성
eigenvector_centrality = nx.eigenvector_centrality(G)
print("위세 중심성:", eigenvector_centrality)

# 페이지랭크 (방향 그래프)
# pagerank = nx.pagerank(G)

# 모든 중심성을 DataFrame으로
import pandas as pd

centrality_df = pd.DataFrame({
    'Degree': degree_centrality,
    'Closeness': closeness_centrality,
    'Betweenness': betweenness_centrality,
    'Eigenvector': eigenvector_centrality
})
print(centrality_df)
```

### 7.3 네트워크 구조 분석

```python
# 밀도
density = nx.density(G)
print(f"밀도: {density:.3f}")

# 클러스터링 계수
clustering = nx.clustering(G)
avg_clustering = nx.average_clustering(G)
print(f"평균 클러스터링 계수: {avg_clustering:.3f}")

# 연결요소
if nx.is_connected(G):
    print("네트워크는 연결되어 있습니다.")
else:
    components = list(nx.connected_components(G))
    print(f"연결요소 수: {len(components)}")
    print(f"최대 연결요소 크기: {len(max(components, key=len))}")

# 평균 경로 길이
if nx.is_connected(G):
    avg_path_length = nx.average_shortest_path_length(G)
    print(f"평균 경로 길이: {avg_path_length:.3f}")

# 지름
if nx.is_connected(G):
    diameter = nx.diameter(G)
    print(f"지름: {diameter}")
```

### 7.4 커뮤니티 탐지

```python
from networkx.algorithms import community

# Louvain 알고리즘
communities = community.louvain_communities(G)
print(f"커뮤니티 수: {len(communities)}")
for i, comm in enumerate(communities):
    print(f"커뮤니티 {i+1}: {comm}")

# 모듈성 계산
modularity = community.modularity(G, communities)
print(f"모듈성: {modularity:.3f}")

# Girvan-Newman
communities_gn = community.girvan_newman(G)
first_level = next(communities_gn)
print(f"Girvan-Newman 커뮤니티: {list(first_level)}")
```

### 7.5 시각화

```python
import matplotlib.pyplot as plt

# 기본 시각화
plt.figure(figsize=(10, 8))
pos = nx.spring_layout(G, seed=42)  # 레이아웃 설정
nx.draw(G, pos, 
        with_labels=True, 
        node_color='lightblue',
        node_size=500,
        font_size=12,
        font_weight='bold',
        edge_color='gray',
        width=2)
plt.title("네트워크 시각화")
plt.show()

# 중심성 기반 시각화
plt.figure(figsize=(12, 10))

# 연결 중심성으로 노드 크기 조정
node_sizes = [v * 3000 for v in degree_centrality.values()]

# 매개 중심성으로 노드 색상 조정
node_colors = [v for v in betweenness_centrality.values()]

nx.draw(G, pos,
        with_labels=True,
        node_size=node_sizes,
        node_color=node_colors,
        cmap='YlOrRd',
        font_size=10,
        font_weight='bold',
        edge_color='gray',
        width=2)
plt.title("중심성 기반 시각화\n(크기: 연결중심성, 색상: 매개중심성)")
plt.colorbar(plt.cm.ScalarMappable(cmap='YlOrRd'), label='매개 중심성')
plt.show()

# 커뮤니티 시각화
plt.figure(figsize=(10, 8))
colors = ['red', 'blue', 'green', 'yellow', 'purple', 'orange']
for i, comm in enumerate(communities):
    nx.draw_networkx_nodes(G, pos, 
                          nodelist=list(comm),
                          node_color=colors[i % len(colors)],
                          node_size=500,
                          label=f'커뮤니티 {i+1}')
nx.draw_networkx_edges(G, pos, edge_color='gray')
nx.draw_networkx_labels(G, pos, font_size=12, font_weight='bold')
plt.title("커뮤니티 탐지 시각화")
plt.legend()
plt.show()
```

### 7.6 실제 데이터 분석 예시

```python
# Karate Club 데이터 (유명한 네트워크 분석 예제)
G = nx.karate_club_graph()

# 기본 정보
print(f"노드 수: {G.number_of_nodes()}")
print(f"엣지 수: {G.number_of_edges()}")
print(f"밀도: {nx.density(G):.3f}")

# 중심성 계산
degree_cent = nx.degree_centrality(G)
between_cent = nx.betweenness_centrality(G)

# 상위 5개 노드 찾기
top_degree = sorted(degree_cent.items(), key=lambda x: x[1], reverse=True)[:5]
top_between = sorted(between_cent.items(), key=lambda x: x[1], reverse=True)[:5]

print("\n연결 중심성 상위 5개:")
for node, cent in top_degree:
    print(f"노드 {node}: {cent:.3f}")

print("\n매개 중심성 상위 5개:")
for node, cent in top_between:
    print(f"노드 {node}: {cent:.3f}")

# 커뮤니티 탐지
communities = community.louvain_communities(G)
print(f"\n탐지된 커뮤니티 수: {len(communities)}")

# 시각화
plt.figure(figsize=(12, 10))
pos = nx.spring_layout(G, seed=42)

# 커뮤니티별 색상
color_map = []
for node in G.nodes():
    for i, comm in enumerate(communities):
        if node in comm:
            color_map.append(i)
            break

nx.draw(G, pos,
        node_color=color_map,
        cmap='Set3',
        with_labels=True,
        node_size=500,
        font_size=10,
        edge_color='gray')
plt.title("Karate Club 네트워크 커뮤니티 분석")
plt.show()
```

### 7.7 CSV 데이터에서 네트워크 생성

```python
import pandas as pd

# 엣지 리스트 CSV 읽기
# CSV 형식: source,target,weight
df = pd.read_csv('network_data.csv')

# 그래프 생성
G = nx.from_pandas_edgelist(df, 
                           source='source',
                           target='target',
                           edge_attr='weight',
                           create_using=nx.Graph())

# 또는 인접행렬에서
# adj_matrix = pd.read_csv('adjacency_matrix.csv', index_col=0)
# G = nx.from_pandas_adjacency(adj_matrix)

# 분석 수행
centrality_df = pd.DataFrame({
    'Node': list(G.nodes()),
    'Degree': list(nx.degree_centrality(G).values()),
    'Betweenness': list(nx.betweenness_centrality(G).values()),
    'Closeness': list(nx.closeness_centrality(G).values())
})

# 결과 저장
centrality_df.to_csv('centrality_results.csv', index=False)
print(centrality_df.head())
```

---

## 8. 빅데이터분석기사 시험 대비

### 8.1 주요 출제 포인트

**1. 중심성 지표 계산**
```
문제 예시:
다음 네트워크에서 노드 C의 연결 중심성은?
A---B---C---D
    |
    E

풀이:
C의 이웃: B, D (2개)
전체 노드: 5개
CD(C) = 2/(5-1) = 0.5
```

**2. 중심성 개념 이해**
- 연결 중심성: 직접 연결 수
- 근접 중심성: 평균 거리 (빨리 도달)
- 매개 중심성: 다리 역할 (정보 통제)
- 위세 중심성: 중요한 사람과 연결

**3. 네트워크 유형 구분**
- 무방향 vs 방향
- 가중치 vs 비가중치
- 이분 네트워크

**4. 구조 지표**
- 밀도 = 2L / n(n-1)
- 클러스터링 계수
- 평균 경로 길이

### 8.2 핵심 공식 정리

```
연결 중심성:
CD(i) = ki / (n-1)

근접 중심성:
CC(i) = (n-1) / Σd(i,j)

밀도:
Density = 2L / (n(n-1))  [무방향]
Density = L / (n(n-1))   [방향]

클러스터링 계수:
Ci = 2ei / (ki(ki-1))
```

### 8.3 실전 문제 유형

**유형 1: 중심성 계산**
```
Q: 다음 그래프에서 매개 중심성이 가장 높은 노드는?
A: 최단 경로들이 가장 많이 지나는 노드
```

**유형 2: 개념 매칭**
```
Q: 정보를 가장 빠르게 전파할 수 있는 위치는?
A: 근접 중심성이 높은 노드
```

**유형 3: 네트워크 특성**
```
Q: 작은 세상 네트워크의 특징은?
A: 낮은 평균 경로 길이 + 높은 클러스터링 계수
```

---

## 요약

### 핵심 포인트

1. **중심성 4가지**:
   - 연결: 직접 연결 많음
   - 근접: 다른 노드와 가까움
   - 매개: 경로상 중요
   - 위세: 중요한 노드와 연결

2. **네트워크 구조**:
   - 밀도: 연결 정도
   - 클러스터링: 응집력
   - 경로 길이: 정보 전파 속도
   - 커뮤니티: 하위 그룹

3. **실전 응용**:
   - SNS: 인플루언서 발굴
   - 조직: 핵심 인물, 병목 지점
   - 추천: 협업 필터링
   - 질병: 전파 경로

4. **Python 도구**:
   - NetworkX: 분석
   - Matplotlib: 시각화
   - Pandas: 데이터 처리

---

**시험 준비 화이팅! 📊**

사회연결망분석은 **관계의 패턴**에서 **의미**를 찾는 것입니다!
