# 통계적 검정 방법 총정리

## 📊 검정법 비교표

| 검정법 | 목적 | 귀무가설 | 검정통계량 | 자유도 | 주요 가정 | Python 함수 |
|--------|------|----------|------------|--------|-----------|-------------|
| **단일표본 t-검정** | 모평균 = 특정값 검정 | μ = μ₀ | t = (x̄ - μ₀)/(s/√n) | **df = n - 1** | 정규성, 독립성 | `stats.ttest_1samp()` |
| **독립표본 t-검정** | 두 집단 평균 비교 | μ₁ = μ₂ | t = (x̄₁ - x̄₂)/√(s²(1/n₁ + 1/n₂)) | **df = n₁ + n₂ - 2** | 정규성, 등분산성 | `stats.ttest_ind()` |
| **대응표본 t-검정** | 쌍을 이루는 데이터 비교 | μd = 0 | t = d̄/(sd/√n) | **df = n - 1** | 차이의 정규성 | `stats.ttest_rel()` |
| **Z-검정** | 모분산 알 때 평균 검정 | μ = μ₀ | Z = (x̄ - μ₀)/(σ/√n) | **자유도 없음** | 정규성, σ 알려짐 | 직접 계산 |
| **F-검정 (등분산)** | 두 집단 분산 비교 | σ₁² = σ₂² | F = s₁²/s₂² | **df₁ = n₁-1, df₂ = n₂-1** | 정규성 | `stats.levene()` |
| **일원분산분석 (ANOVA)** | 3개 이상 집단 평균 비교 | μ₁ = μ₂ = μ₃ = ... | F = MSB/MSW | **df₁ = k-1, df₂ = N-k** | 정규성, 등분산성, 독립성 | `stats.f_oneway()` |
| **카이제곱 적합도 검정** | 분포 적합성 검정 | 관측빈도 = 기대빈도 | χ² = Σ(Oi - Ei)²/Ei | **df = k - 1** | 기대빈도 ≥ 5 | `stats.chisquare()` |
| **카이제곱 독립성 검정** | 두 범주형 변수 독립성 | 두 변수 독립 | χ² = Σ(Oi - Ei)²/Ei | **df = (r-1)(c-1)** | 기대빈도 ≥ 5 | `stats.chi2_contingency()` |
| **카이제곱 동질성 검정** | 여러 집단의 분포 비교 | 분포가 동일 | χ² = Σ(Oi - Ei)²/Ei | **df = (r-1)(c-1)** | 기대빈도 ≥ 5 | `stats.chi2_contingency()` |

---

## 1️⃣ 단일표본 t-검정 (One Sample t-test)

### 📌 개념
- **목적**: 한 집단의 평균이 특정 값과 같은지 검정
- **사용시기**: 모분산을 모를 때, 표본크기 작을 때

### 📐 검정통계량 및 자유도
```
t = (x̄ - μ₀) / (s / √n)

x̄: 표본평균
μ₀: 검정하려는 모평균
s: 표본표준편차
n: 표본크기

자유도: df = n - 1
```

### 🎯 가정
1. **정규성**: 데이터가 정규분포를 따름 (n≥30이면 완화)
2. **독립성**: 각 관측치는 독립적
3. **측정 수준**: 연속형 데이터

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 새로운 학습법의 효과 검정
# H₀: μ = 70 (기존 방법과 같음)
# H₁: μ > 70 (새 방법이 더 좋음, 단측검정)

sample = [75, 78, 72, 80, 76, 74, 79, 73, 77, 81]

# 기술통계
print("표본크기:", len(sample))
print("표본평균:", np.mean(sample))
print("표본표준편차:", np.std(sample, ddof=1))

# t-검정 수행
t_statistic, p_value = stats.ttest_1samp(sample, 70)

# 단측검정 p-value 계산 (μ > 70)
p_value_one_sided = p_value / 2 if t_statistic > 0 else 1 - p_value / 2

print("\n[검정 결과]")
print(f"t-통계량: {t_statistic:.4f}")
print(f"p-value (양측): {p_value:.4f}")
print(f"p-value (단측): {p_value_one_sided:.4f}")
print(f"자유도: {len(sample)-1}")

# 결론
alpha = 0.05
if p_value_one_sided < alpha:
    print(f"\n결론: p-value({p_value_one_sided:.4f}) < α({alpha})")
    print("귀무가설 기각. 새 학습법이 효과가 있습니다.")
else:
    print(f"\n결론: p-value({p_value_one_sided:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 새 학습법의 효과가 없습니다.")
```

**출력 예시:**
```
표본크기: 10
표본평균: 76.5
표본표준편차: 3.0277

[검정 결과]
t-통계량: 6.7882
p-value (양측): 0.0001
p-value (단측): 0.00005
자유도: 9

결론: p-value(0.00005) < α(0.05)
귀무가설 기각. 새 학습법이 효과가 있습니다.
```

---

## 2️⃣ 독립표본 t-검정 (Independent Two Sample t-test)

### 📌 개념
- **목적**: 두 독립된 집단의 평균 비교
- **사용시기**: 두 집단이 서로 독립적일 때

### 📐 검정통계량 및 자유도
```
# 등분산 가정 (Pooled variance)
t = (x̄₁ - x̄₂) / √(s²pooled × (1/n₁ + 1/n₂))

s²pooled = [(n₁-1)s₁² + (n₂-1)s₂²] / (n₁ + n₂ - 2)

자유도: df = n₁ + n₂ - 2

# 등분산 가정하지 않음 (Welch's t-test)
t = (x̄₁ - x̄₂) / √(s₁²/n₁ + s₂²/n₂)

자유도 (Welch-Satterthwaite): 
df = (s₁²/n₁ + s₂²/n₂)² / [(s₁²/n₁)²/(n₁-1) + (s₂²/n₂)²/(n₂-1)]
```

### 🎯 가정
1. **정규성**: 각 집단이 정규분포
2. **독립성**: 두 집단이 서로 독립
3. **등분산성**: 두 집단의 분산이 같음 (Welch's t-test는 불필요)

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 두 학습법 비교
# H₀: μ₁ = μ₂ (두 방법의 효과가 같음)
# H₁: μ₁ ≠ μ₂ (두 방법의 효과가 다름)

method_A = [75, 78, 72, 80, 76, 74, 79, 73, 77, 81]
method_B = [68, 71, 69, 73, 70, 72, 74, 67, 69, 71]

# 기술통계
print("방법 A - 평균:", np.mean(method_A), "표준편차:", np.std(method_A, ddof=1))
print("방법 B - 평균:", np.mean(method_B), "표준편차:", np.std(method_B, ddof=1))

# 등분산 검정 (Levene's test)
stat_levene, p_levene = stats.levene(method_A, method_B)
print(f"\n[등분산 검정]")
print(f"Levene 통계량: {stat_levene:.4f}, p-value: {p_levene:.4f}")

# 독립표본 t-검정
# equal_var=True: 등분산 가정, equal_var=False: Welch's t-test
t_stat, p_value = stats.ttest_ind(method_A, method_B, equal_var=True)

print(f"\n[독립표본 t-검정]")
print(f"t-통계량: {t_stat:.4f}")
print(f"p-value: {p_value:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 두 방법의 효과가 다릅니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 두 방법의 효과가 같습니다.")
```

---

## 3️⃣ 대응표본 t-검정 (Paired Sample t-test)

### 📌 개념
- **목적**: 같은 대상에 대한 전후 비교 (before-after)
- **사용시기**: 쌍을 이루는 데이터, 반복측정

### 📐 검정통계량 및 자유도
```
t = d̄ / (sd / √n)

d̄: 차이의 평균
sd: 차이의 표준편차
n: 쌍의 개수

자유도: df = n - 1
```

### 🎯 가정
1. **차이의 정규성**: 차이값이 정규분포
2. **쌍의 독립성**: 각 쌍은 독립적

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 약물 치료 전후 비교
# H₀: μd = 0 (치료 효과 없음)
# H₁: μd ≠ 0 (치료 효과 있음)

before = [110, 115, 120, 108, 112, 118, 122, 105, 114, 119]
after = [105, 110, 112, 102, 108, 112, 115, 100, 109, 113]

# 차이 계산
diff = np.array(after) - np.array(before)

print("차이 (after - before):", diff)
print("차이의 평균:", np.mean(diff))
print("차이의 표준편차:", np.std(diff, ddof=1))

# 대응표본 t-검정
t_stat, p_value = stats.ttest_rel(after, before)

print(f"\n[대응표본 t-검정]")
print(f"t-통계량: {t_stat:.4f}")
print(f"p-value: {p_value:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 약물 치료 효과가 있습니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 약물 치료 효과가 없습니다.")
```

---

## 4️⃣ Z-검정 (Z-test)

### 📌 개념
- **목적**: 모분산을 알 때 평균 검정
- **사용시기**: n≥30 또는 모분산(σ)을 알 때

### 📐 검정통계량 및 자유도
```
Z = (x̄ - μ₀) / (σ / √n)

x̄: 표본평균
μ₀: 검정하려는 모평균
σ: 모표준편차 (알려진 값)
n: 표본크기

자유도: 없음 (표준정규분포 N(0,1) 사용)
```

### 🎯 가정
1. **정규성**: 모집단이 정규분포 또는 n≥30
2. **모분산 알려짐**: σ를 알고 있음

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 공장 제품 무게 검정
# H₀: μ = 500g (규정 무게)
# H₁: μ ≠ 500g (규정 무게와 다름)

sample = [502, 498, 503, 501, 499, 497, 504, 500, 502, 501]
mu_0 = 500  # 검정하려는 모평균
sigma = 2  # 알려진 모표준편차

n = len(sample)
x_bar = np.mean(sample)

# Z-통계량 계산
z_stat = (x_bar - mu_0) / (sigma / np.sqrt(n))

# p-value 계산 (양측검정)
p_value = 2 * (1 - stats.norm.cdf(abs(z_stat)))

print(f"표본크기: {n}")
print(f"표본평균: {x_bar:.2f}")
print(f"모표준편차: {sigma}")
print(f"\n[Z-검정 결과]")
print(f"Z-통계량: {z_stat:.4f}")
print(f"p-value: {p_value:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 제품 무게가 규정과 다릅니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 제품 무게가 규정과 같습니다.")
```

---

## 5️⃣ F-검정 (등분산 검정, F-test for Equality of Variances)

### 📌 개념
- **목적**: 두 집단의 분산이 같은지 검정
- **사용시기**: t-검정 전 등분산성 확인

### 📐 검정통계량 및 자유도
```
F = s₁² / s₂²  (s₁² > s₂²)

s₁²: 더 큰 표본분산
s₂²: 더 작은 표본분산

자유도: 
df₁ = n₁ - 1 (분자의 자유도)
df₂ = n₂ - 1 (분모의 자유도)
```

### 🎯 가정
1. **정규성**: 두 집단 모두 정규분포
2. **독립성**: 두 집단이 독립적

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 두 기계의 생산품 품질 분산 비교
# H₀: σ₁² = σ₂² (두 기계의 분산이 같음)
# H₁: σ₁² ≠ σ₂² (두 기계의 분산이 다름)

machine_1 = [23, 25, 22, 24, 26, 23, 25, 24, 22, 25]
machine_2 = [20, 28, 19, 30, 21, 27, 22, 29, 20, 28]

var_1 = np.var(machine_1, ddof=1)
var_2 = np.var(machine_2, ddof=1)

print(f"기계1 분산: {var_1:.4f}")
print(f"기계2 분산: {var_2:.4f}")

# F-검정 (Levene's test 사용 - 더 robust)
stat_levene, p_value = stats.levene(machine_1, machine_2)

print(f"\n[Levene의 등분산 검정]")
print(f"검정통계량: {stat_levene:.4f}")
print(f"p-value: {p_value:.4f}")

# 직접 F-검정
f_stat = var_1 / var_2 if var_1 > var_2 else var_2 / var_1
df1 = len(machine_1) - 1
df2 = len(machine_2) - 1
p_value_f = 2 * min(stats.f.cdf(f_stat, df1, df2), 
                    1 - stats.f.cdf(f_stat, df1, df2))

print(f"\n[F-검정]")
print(f"F-통계량: {f_stat:.4f}")
print(f"자유도: ({df1}, {df2})")
print(f"p-value: {p_value_f:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 두 기계의 분산이 다릅니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 두 기계의 분산이 같습니다.")
```

---

## 6️⃣ 일원분산분석 (One-way ANOVA)

### 📌 개념
- **목적**: 3개 이상 집단의 평균 비교
- **사용시기**: 독립변수 1개, 종속변수 연속형

### 📐 검정통계량 및 자유도
```
F = MSB / MSW

MSB = SSB / df_between  # 집단 간 평균제곱
MSW = SSW / df_within   # 집단 내 평균제곱

k: 집단 수
N: 전체 표본 수
n: 각 집단의 표본 수

자유도:
df_between (집단 간) = k - 1
df_within (집단 내) = N - k
df_total (전체) = N - 1
```

### 🎯 가정
1. **정규성**: 각 집단이 정규분포
2. **등분산성**: 각 집단의 분산이 같음
3. **독립성**: 관측치가 독립적

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 3가지 교육 방법의 효과 비교
# H₀: μ₁ = μ₂ = μ₃ (세 방법의 효과가 같음)
# H₁: 적어도 하나는 다름

method_A = [85, 88, 82, 90, 87, 84, 86, 89]
method_B = [78, 81, 79, 83, 80, 82, 77, 84]
method_C = [92, 95, 91, 94, 93, 96, 90, 97]

# 기술통계
print("방법 A 평균:", np.mean(method_A))
print("방법 B 평균:", np.mean(method_B))
print("방법 C 평균:", np.mean(method_C))

# 일원분산분석
f_stat, p_value = stats.f_oneway(method_A, method_B, method_C)

print(f"\n[일원분산분석 결과]")
print(f"F-통계량: {f_stat:.4f}")
print(f"p-value: {p_value:.6f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.6f}) < α({alpha})")
    print("귀무가설 기각. 적어도 한 교육 방법의 효과가 다릅니다.")
    print("→ 사후분석(Tukey HSD 등) 필요")
else:
    print(f"\n결론: p-value({p_value:.6f}) ≥ α({alpha})")
    print("귀무가설 채택. 세 교육 방법의 효과가 같습니다.")

# 사후분석 (Tukey HSD)
from statsmodels.stats.multicomp import pairwise_tukeyhsd

# 데이터 재구성
data = np.concatenate([method_A, method_B, method_C])
groups = ['A']*len(method_A) + ['B']*len(method_B) + ['C']*len(method_C)

print("\n[Tukey HSD 사후분석]")
tukey_result = pairwise_tukeyhsd(data, groups, alpha=0.05)
print(tukey_result)
```

---

## 7️⃣ 카이제곱 적합도 검정 (Chi-square Goodness of Fit Test)

### 📌 개념
- **목적**: 관측빈도가 기대빈도와 일치하는지 검정
- **사용시기**: 범주형 데이터의 분포 적합성

### 📐 검정통계량 및 자유도
```
χ² = Σ [(Oi - Ei)² / Ei]

Oi: 관측빈도
Ei: 기대빈도
k: 범주의 개수

자유도: df = k - 1
```

### 🎯 가정
1. **기대빈도**: 각 범주의 기대빈도 ≥ 5
2. **독립성**: 관측치가 독립적
3. **무작위 표본**: 표본이 무작위로 추출됨

### 📊 Python 예제

```python
import numpy as np
from scipy import stats

# 예제: 주사위의 공정성 검정
# H₀: 주사위가 공정함 (각 면이 1/6 확률)
# H₁: 주사위가 공정하지 않음

# 관측빈도 (주사위를 120번 던진 결과)
observed = [18, 22, 19, 21, 20, 20]  # 1~6까지
expected = [20, 20, 20, 20, 20, 20]  # 공정한 주사위의 기대빈도

print("관측빈도:", observed)
print("기대빈도:", expected)

# 카이제곱 적합도 검정
chi2_stat, p_value = stats.chisquare(observed, expected)

print(f"\n[카이제곱 적합도 검정]")
print(f"χ² 통계량: {chi2_stat:.4f}")
print(f"자유도: {len(observed)-1}")
print(f"p-value: {p_value:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 주사위가 공정하지 않습니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 주사위가 공정합니다.")
```

---

## 8️⃣ 카이제곱 독립성 검정 (Chi-square Test of Independence)

### 📌 개념
- **목적**: 두 범주형 변수가 독립인지 검정
- **사용시기**: 교차표(contingency table) 분석

### 📐 검정통계량 및 자유도
```
χ² = Σ [(Oi - Ei)² / Ei]

Ei = (행 합계 × 열 합계) / 전체 합계

r: 행의 개수
c: 열의 개수

자유도: df = (r - 1) × (c - 1)
```

### 🎯 가정
1. **기대빈도**: 기대빈도 ≥ 5 (80% 이상의 셀)
2. **독립성**: 관측치가 독립적
3. **무작위 표본**: 표본이 무작위로 추출됨

### 📊 Python 예제

```python
import numpy as np
from scipy import stats
import pandas as pd

# 예제: 성별과 제품 선호도의 독립성
# H₀: 성별과 제품 선호도는 독립이다
# H₁: 성별과 제품 선호도는 독립이 아니다

# 교차표 (성별 × 제품)
data = np.array([
    [30, 20, 10],  # 남성: A, B, C 제품
    [15, 25, 30]   # 여성: A, B, C 제품
])

# DataFrame으로 보기 좋게 출력
df = pd.DataFrame(data, 
                  index=['남성', '여성'],
                  columns=['제품A', '제품B', '제품C'])
print("교차표:")
print(df)
print("\n합계:")
print("행 합계:", data.sum(axis=1))
print("열 합계:", data.sum(axis=0))
print("전체 합계:", data.sum())

# 카이제곱 독립성 검정
chi2_stat, p_value, dof, expected = stats.chi2_contingency(data)

print(f"\n[카이제곱 독립성 검정]")
print(f"χ² 통계량: {chi2_stat:.4f}")
print(f"자유도: {dof}")
print(f"p-value: {p_value:.4f}")

print("\n기대빈도:")
print(pd.DataFrame(expected, 
                   index=['남성', '여성'],
                   columns=['제품A', '제품B', '제품C']))

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 성별과 제품 선호도는 독립이 아닙니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 성별과 제품 선호도는 독립입니다.")
```

---

## 9️⃣ 카이제곱 동질성 검정 (Chi-square Test of Homogeneity)

### 📌 개념
- **목적**: 여러 집단의 범주형 분포가 동일한지 검정
- **사용시기**: 여러 모집단의 비율 비교

### 📐 검정통계량 및 자유도
```
χ² = Σ [(Oi - Ei)² / Ei]

(독립성 검정과 동일한 공식, 해석이 다름)

r: 행의 개수 (집단 수)
c: 열의 개수 (범주 수)

자유도: df = (r - 1) × (c - 1)
```

### 🎯 가정
- 독립성 검정과 동일

### 📊 Python 예제

```python
import numpy as np
from scipy import stats
import pandas as pd

# 예제: 세 지역의 정당 지지도 비교
# H₀: 세 지역의 정당 지지 분포가 동일하다
# H₁: 세 지역의 정당 지지 분포가 다르다

# 교차표 (지역 × 정당)
data = np.array([
    [45, 30, 25],  # 서울: A당, B당, C당
    [50, 35, 15],  # 부산
    [40, 40, 20]   # 대구
])

df = pd.DataFrame(data,
                  index=['서울', '부산', '대구'],
                  columns=['A당', 'B당', 'C당'])
print("교차표:")
print(df)

# 카이제곱 동질성 검정
chi2_stat, p_value, dof, expected = stats.chi2_contingency(data)

print(f"\n[카이제곱 동질성 검정]")
print(f"χ² 통계량: {chi2_stat:.4f}")
print(f"자유도: {dof}")
print(f"p-value: {p_value:.4f}")

alpha = 0.05
if p_value < alpha:
    print(f"\n결론: p-value({p_value:.4f}) < α({alpha})")
    print("귀무가설 기각. 세 지역의 정당 지지 분포가 다릅니다.")
else:
    print(f"\n결론: p-value({p_value:.4f}) ≥ α({alpha})")
    print("귀무가설 채택. 세 지역의 정당 지지 분포가 동일합니다.")
```

---

## 🎯 검정법 선택 플로우차트

```
데이터 유형이 무엇인가?
│
├─ 연속형 (평균 비교)
│  │
│  ├─ 비교할 집단 수는?
│  │  │
│  │  ├─ 1개 집단 vs 특정값
│  │  │  │
│  │  │  ├─ 모분산 알려짐? → Z-검정
│  │  │  └─ 모분산 모름? → 단일표본 t-검정
│  │  │
│  │  ├─ 2개 집단
│  │  │  │
│  │  │  ├─ 독립된 집단? → 독립표본 t-검정
│  │  │  └─ 쌍을 이루는 데이터? → 대응표본 t-검정
│  │  │
│  │  └─ 3개 이상 집단 → 일원분산분석 (ANOVA)
│  │
│  └─ 분산 비교 → F-검정
│
└─ 범주형 (빈도 비교)
   │
   ├─ 1개 변수의 분포 → 카이제곱 적합도 검정
   ├─ 2개 변수의 관계 → 카이제곱 독립성 검정
   └─ 여러 집단의 분포 비교 → 카이제곱 동질성 검정
```

---

## 📋 검정 수행 체크리스트

### 1. 검정 전 확인사항
- [ ] 연구 질문이 명확한가?
- [ ] 귀무가설과 대립가설을 설정했는가?
- [ ] 유의수준(α)을 결정했는가?
- [ ] 적절한 검정 방법을 선택했는가?

### 2. 가정 검토
- [ ] 정규성 검정 (Shapiro-Wilk, Q-Q plot)
- [ ] 등분산성 검정 (Levene's test)
- [ ] 독립성 확인
- [ ] 표본크기 충분성

### 3. 검정 수행
- [ ] 검정통계량 계산
- [ ] p-value 계산
- [ ] 임계값과 비교

### 4. 결과 해석
- [ ] 통계적 유의성 판단
- [ ] 실질적 유의성(effect size) 고려
- [ ] 결과를 명확하게 서술

---

## 🔍 정규성 검정

### Shapiro-Wilk 검정

```python
from scipy import stats

# H₀: 데이터가 정규분포를 따른다
# H₁: 데이터가 정규분포를 따르지 않는다

data = [75, 78, 72, 80, 76, 74, 79, 73, 77, 81]

stat, p_value = stats.shapiro(data)
print(f"Shapiro-Wilk 통계량: {stat:.4f}")
print(f"p-value: {p_value:.4f}")

if p_value > 0.05:
    print("정규성 가정을 만족합니다.")
else:
    print("정규성 가정을 만족하지 않습니다.")
```

### Q-Q Plot

```python
import matplotlib.pyplot as plt
from scipy import stats

data = [75, 78, 72, 80, 76, 74, 79, 73, 77, 81]

stats.probplot(data, dist="norm", plot=plt)
plt.title("Q-Q Plot")
plt.grid(True)
plt.show()
```

---

## 🔢 자유도 (Degrees of Freedom) 완전 정리

### 📌 자유도란?
**자유도(df)**는 통계량을 계산할 때 **자유롭게 변할 수 있는 값의 개수**입니다.

### 💡 왜 자유도가 중요한가?
1. **임계값 결정**: 검정통계량의 분포를 결정하여 임계값을 찾음
2. **p-value 계산**: 자유도에 따라 확률 분포가 달라짐
3. **검정력**: 자유도가 클수록 추정의 정확도가 높아짐

### 📊 검정법별 자유도 요약표

| 검정법 | 자유도 공식 | 설명 |
|--------|-------------|------|
| **단일표본 t-검정** | df = n - 1 | n: 표본크기, 평균 1개 추정으로 1 손실 |
| **독립표본 t-검정** | df = n₁ + n₂ - 2 | 두 집단 평균 추정으로 2 손실 |
| **독립표본 t (Welch)** | df = (s₁²/n₁+s₂²/n₂)² / [(s₁²/n₁)²/(n₁-1)+(s₂²/n₂)²/(n₂-1)] | Welch-Satterthwaite 근사 |
| **대응표본 t-검정** | df = n - 1 | n: 쌍의 개수, 차이의 평균 1개 추정 |
| **Z-검정** | 없음 | 표준정규분포(N(0,1)) 사용 |
| **F-검정 (등분산)** | df₁ = n₁-1, df₂ = n₂-1 | 두 집단의 각각 1개씩 추정 |
| **ANOVA** | df₁ = k-1, df₂ = N-k | k: 집단수, N: 전체 표본수 |
| **카이제곱 적합도** | df = k - 1 | k: 범주 개수, 전체 합이 고정 |
| **카이제곱 독립성** | df = (r-1)(c-1) | r: 행수, c: 열수, 주변합 고정 |
| **카이제곱 동질성** | df = (r-1)(c-1) | r: 집단수, c: 범주수 |

### 🎓 자유도 계산 원리

#### 예제 1: 단일표본 t-검정 (n=5)
```
데이터: [10, 12, 14, 16, 18]
평균 x̄ = 14

만약 처음 4개 값이 [10, 12, 14, 16]이면,
평균이 14가 되려면 마지막 값은 반드시 18이어야 함
→ 자유롭게 선택 가능한 값: 4개
→ df = 5 - 1 = 4
```

#### 예제 2: 카이제곱 적합도 검정 (k=6, 주사위)
```
범주: [1, 2, 3, 4, 5, 6]
관측빈도: [18, 22, 19, 21, 20, ?]
전체 합: 120

처음 5개 값이 정해지면, 마지막 값은 자동으로 결정됨
→ 자유롭게 선택 가능한 범주: 5개
→ df = 6 - 1 = 5
```

#### 예제 3: 카이제곱 독립성 검정 (2×3 교차표)
```
        A    B    C   | 행합
남성    30   20   10  | 60
여성    ?    ?    ?   | 70
--------|------------|----
열합    45   45   40  | 130

행합과 열합이 모두 고정되면:
- 첫 행의 A, B를 정하면 C는 자동 결정
- 첫 행이 정해지면 둘째 행도 자동 결정
→ 자유롭게 선택 가능: (2-1) × (3-1) = 2개
→ df = (2-1)(3-1) = 2
```

### 📈 자유도와 분포의 관계

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy import stats

# t-분포: 자유도에 따른 변화
x = np.linspace(-4, 4, 1000)
dfs = [1, 3, 10, 30, 100]

plt.figure(figsize=(10, 6))
for df in dfs:
    plt.plot(x, stats.t.pdf(x, df), label=f'df={df}')
plt.plot(x, stats.norm.pdf(x), 'k--', label='표준정규분포', linewidth=2)
plt.legend()
plt.title('자유도에 따른 t-분포의 변화')
plt.xlabel('t')
plt.ylabel('확률밀도')
plt.grid(True)
plt.show()

# df가 커질수록 표준정규분포에 가까워짐!
```

### ⚠️ 자유도 관련 주의사항

1. **표본표준편차 계산 시**
   - Python: `np.std(data, ddof=1)` ← **ddof=1 필수!**
   - ddof=0: 모표준편차 (N으로 나눔)
   - ddof=1: 표본표준편차 (N-1로 나눔)

2. **자유도가 작을 때**
   - 분포의 꼬리가 두꺼워짐 (heavy tail)
   - 극단값에 대한 확률이 높아짐
   - 임계값이 커짐 → 검정이 더 보수적

3. **자유도와 검정력**
   - 자유도 ↑ → 표준오차 ↓ → 검정력 ↑
   - 표본크기를 늘리면 자유도도 증가

### 💻 Python에서 자유도 확인

```python
from scipy import stats
import numpy as np

# 1. 단일표본 t-검정
data = [75, 78, 72, 80, 76]
t_stat, p_value = stats.ttest_1samp(data, 70)
df = len(data) - 1
print(f"단일표본 t-검정: df = {df}")

# 2. 독립표본 t-검정
group1 = [75, 78, 72, 80, 76]
group2 = [68, 71, 69, 73, 70]
t_stat, p_value = stats.ttest_ind(group1, group2)
df = len(group1) + len(group2) - 2
print(f"독립표본 t-검정: df = {df}")

# 3. 카이제곱 검정
observed = [18, 22, 19, 21, 20, 20]
chi2_stat, p_value = stats.chisquare(observed)
df = len(observed) - 1
print(f"카이제곱 적합도: df = {df}")

# 4. 카이제곱 독립성 검정
contingency = np.array([[30, 20, 10],
                         [15, 25, 30]])
chi2, p_value, df, expected = stats.chi2_contingency(contingency)
print(f"카이제곱 독립성: df = {df}")
# df = (2-1) × (3-1) = 2
```

---

## 📚 주요 용어 정리

| 용어 | 설명 |
|------|------|
| **귀무가설 (H₀)** | 차이가 없다고 가정하는 가설 |
| **대립가설 (H₁)** | 차이가 있다고 주장하는 가설 |
| **유의수준 (α)** | 1종 오류를 범할 최대 허용 확률 (보통 0.05) |
| **p-value** | 귀무가설이 참일 때 관측된 결과보다 극단적인 결과가 나올 확률 |
| **검정통계량** | 가설검정을 위해 계산한 값 (t, F, Z, χ² 등) |
| **자유도 (df)** | 통계량을 계산할 때 자유롭게 변할 수 있는 값의 개수. 검정통계량의 분포를 결정함 |
| **1종 오류 (α)** | H₀가 참인데 기각하는 오류 (false positive) |
| **2종 오류 (β)** | H₀가 거짓인데 채택하는 오류 (false negative) |
| **검정력 (1-β)** | 대립가설이 참일 때 이를 정확히 탐지할 확률 |
| **단측검정** | 한쪽 방향으로만 검정 (>, <) |
| **양측검정** | 양쪽 방향 모두 검정 (≠) |

---

## 💡 실전 팁

### 1. 검정법 선택 가이드
- **n < 30**: t-검정 (정규성 확인 필요)
- **n ≥ 30**: t-검정 또는 Z-검정 (중심극한정리)
- **분산 모름**: t-검정
- **분산 알 때**: Z-검정

### 2. p-value 해석
- **p < 0.01**: 매우 강한 증거 (highly significant)
- **0.01 ≤ p < 0.05**: 유의함 (significant)
- **0.05 ≤ p < 0.10**: 약한 증거 (marginally significant)
- **p ≥ 0.10**: 유의하지 않음 (not significant)

### 3. 효과크기 (Effect Size)
- **Cohen's d**: 두 집단 평균 차이의 표준화된 크기
  - 작은 효과: d = 0.2
  - 중간 효과: d = 0.5
  - 큰 효과: d = 0.8

```python
def cohens_d(group1, group2):
    n1, n2 = len(group1), len(group2)
    var1, var2 = np.var(group1, ddof=1), np.var(group2, ddof=1)
    pooled_std = np.sqrt(((n1-1)*var1 + (n2-1)*var2) / (n1+n2-2))
    return (np.mean(group1) - np.mean(group2)) / pooled_std
```

### 4. 다중비교 문제
- 여러 번의 검정을 수행하면 1종 오류가 증가
- **Bonferroni 보정**: α' = α / m (m: 검정 횟수)
- **FDR (False Discovery Rate)**: Benjamini-Hochberg 절차

---

## 🎓 시험 대비 핵심 요약

### 반드시 암기할 공식

**검정통계량:**
1. **t-통계량**: t = (x̄ - μ₀) / (s/√n)
2. **Z-통계량**: Z = (x̄ - μ₀) / (σ/√n)
3. **카이제곱**: χ² = Σ(Oi - Ei)²/Ei
4. **F-통계량**: F = s₁²/s₂² 또는 F = MSB/MSW

**자유도:**
1. **단일표본 t**: df = n - 1
2. **독립표본 t**: df = n₁ + n₂ - 2
3. **대응표본 t**: df = n - 1
4. **F-검정**: df₁ = n₁-1, df₂ = n₂-1
5. **ANOVA**: df₁ = k-1, df₂ = N-k
6. **카이제곱 적합도**: df = k - 1
7. **카이제곱 독립성/동질성**: df = (r-1)(c-1)

### 검정법별 핵심 키워드

| 검정법 | 핵심 키워드 | 자유도 |
|--------|-------------|--------|
| t-검정 | 평균, 정규성, 모분산 모름, n<30 | n-1 또는 n₁+n₂-2 |
| Z-검정 | 평균, 모분산 알 때, n≥30 | 없음 (표준정규분포) |
| F-검정 | 분산, 등분산성, ANOVA | (n₁-1, n₂-1) 또는 (k-1, N-k) |
| χ²-검정 | 범주형, 빈도, 기대빈도≥5 | k-1 또는 (r-1)(c-1) |

---

## 📖 참고자료

- scipy.stats 공식 문서: https://docs.scipy.org/doc/scipy/reference/stats.html
- statsmodels 공식 문서: https://www.statsmodels.org/
- Khan Academy - Statistics: https://www.khanacademy.org/math/statistics-probability

---

**작성일**: 2025-02-06  
**버전**: 1.0  
**용도**: 빅데이터분석기사 시험 대비 통계적 검정 방법 총정리
